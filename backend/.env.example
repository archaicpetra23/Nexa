# Database
DB_DSN="host=localhost user=postgres password=CHANGE_ME dbname=nexa port=5432 sslmode=disable"

# JWT (min 32 chars, generate with: openssl rand -base64 32)
JWT_SECRET="CHANGE_ME_MIN_32_CHARS"

# Server
SERVER_PORT=":8080"

# Cookie
COOKIE_DOMAIN="localhost"
COOKIE_SECURE="false"